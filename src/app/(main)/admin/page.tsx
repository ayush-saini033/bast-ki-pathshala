import ProfilesPage from "@/features/admin/components/profile"

export const metadata = {
  title: "Admin",
  description: "Admin can see all the resgistration for volunteer and inter",
};

const AdminPage = () => {
return (
  <ProfilesPage/>
)
}

export default AdminPage